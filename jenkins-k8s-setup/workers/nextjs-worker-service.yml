apiVersion: v1
kind: Service
metadata:
  name: nextjs-worker-service
  namespace: slinkylinky
spec:
  selector:
    app: nextjs-worker
  ports:
    - protocol: TCP
      port: 50000
      targetPort: 50000
  type: ClusterIP