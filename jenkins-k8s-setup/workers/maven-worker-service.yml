apiVersion: v1
kind: Service
metadata:
  name: maven-worker-service
  namespace: slinkylinky
spec:
  selector:
    app: maven-worker
  ports:
    - protocol: TCP
      port: 50000
      targetPort: 50000
  type: ClusterIP