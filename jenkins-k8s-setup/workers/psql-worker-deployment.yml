apiVersion: apps/v1
kind: Deployment
metadata:
  name: plsql-worker
  namespace: slinkylinky
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plsql-worker
      version: v1
  template:
    metadata:
      labels:
        app: plsql-worker
        version: v1
    spec:
      containers:
        - name: plsql-worker
          image: rdomloge/jenkins-worker-psql:1.0.0
          imagePullPolicy: IfNotPresent
          env:
            - name: jenkins_URL
              value: http://jenkins-service:8080
            - name: jenkins_SECRET
              value: CHANGEME
            - name: jenkins_AGENT_NAME
              value: psql-worker
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: psql-worker-home
              mountPath: /home/jenkins
      volumes:
        - name: psql-worker-home
          emptyDir: {}