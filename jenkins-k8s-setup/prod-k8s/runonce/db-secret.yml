apiVersion: v1
kind: Secret
metadata:
  name: db-connection-secret
  namespace: slinkylinky
type: Opaque
data:
  DATABASE_URL: "${JDBC_CONNECTION_STRING}"  # Replace with your actual JDBC connection string
# DATABASE_URL is base64 encoded: postgres://user:password@db-host:5432/db?ssl